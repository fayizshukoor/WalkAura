<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>500 - Server Error | WalkAura</title>
    <style>
        /* Reuse same styles for consistency */
        body {
            margin: 0;
            background-color: #0b0c10;
            color: #ffffff;
            font-family: 'Inter', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
        }
        .error-code {
            font-size: 8rem;
            font-weight: 800;
            margin: 0;
            color: #ff5047;
            line-height: 1;
        }
        h1 { font-size: 2.5rem; margin: 10px 0; }
        p { color: #a0a0a0; font-size: 1.1rem; margin-bottom: 30px; }
        .home-button {
            display: inline-block;
            background-color: #4f46e5;
            color: white;
            padding: 14px 28px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <p class="error-code">500</p>
        <h1>Server Error.</h1>
        <p>Something went wrong on our end. We're working to fix it. Please try again later.</p>
        <a href="/" class="home-button">Back to Home</a>
    </div>
</body>
</html>