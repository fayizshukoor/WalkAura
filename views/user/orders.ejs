<main class="flex-grow pt-24 pb-20 px-4 sm:px-6 lg:px-8">
    <div class="max-w-[1250px] mx-auto space-y-10">
        
        <div class="space-y-3">
            <nav class="flex items-center gap-2 text-xs font-bold uppercase tracking-widest mb-4">
                <a href="/" class="text-gray-400 hover:text-primary transition-colors">Home</a>
                <span class="text-gray-600">/</span>
                <span class="text-primary">My Orders</span>
            </nav>
            <h1 class="text-4xl sm:text-5xl font-extrabold tracking-tight uppercase text-white">My Orders</h1>
            <p class="text-gray-400 text-sm font-medium tracking-wide">Manage your history and track current shipments.</p>
        </div>

        <section class="bg-[#12141a] border border-white/10 rounded-[2rem] p-5 sm:p-7 shadow-2xl">
            <form action="/orders" method="GET" id="filterForm" class="flex flex-col lg:flex-row gap-5">
                <div class="relative flex-grow">
                    <span class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-2xl">search</span>
                    <input type="text" name="search" value="<%= filters.search %>"
                        placeholder="Search Order ID or Product..." 
                        class="w-full bg-black border border-white/10 rounded-xl py-4 pl-14 pr-12 text-white text-sm font-medium focus:outline-none focus:border-primary focus:ring-1 focus:ring-primary/30 transition-all placeholder:text-gray-600">
                    
                    <% if (filters.search) { %>
                        <button type="button" onclick="window.location.href='/orders'" class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-500 hover:text-white transition-colors">
                            <span class="material-symbols-outlined text-xl">close</span>
                        </button>
                    <% } %>
                </div>

                <div class="w-full lg:w-56">
                    <select name="status" onchange="this.form.submit()" 
                        class="w-full bg-black border border-white/10 rounded-xl py-4 px-5 text-white text-xs font-bold uppercase tracking-widest appearance-none focus:outline-none focus:border-primary cursor-pointer hover:border-white/20 transition-all">
                        <option value="all" <%= filters.status === 'all' ? 'selected' : '' %>>All Statuses</option>
                        <option value="Pending" <%= filters.status === 'Pending' ? 'selected' : '' %>>Pending</option>
                        <option value="Shipped" <%= filters.status === 'Shipped' ? 'selected' : '' %>>Shipped</option>
                        <option value="Delivered" <%= filters.status === 'Delivered' ? 'selected' : '' %>>Delivered</option>
                        <option value="Cancelled" <%= filters.status === 'Cancelled' ? 'selected' : '' %>>Cancelled</option>
                    </select>
                </div>

                <div class="w-full lg:w-56">
                    <select name="sort" onchange="this.form.submit()" 
                        class="w-full bg-black border border-white/10 rounded-xl py-4 px-5 text-white text-xs font-bold uppercase tracking-widest appearance-none focus:outline-none focus:border-primary cursor-pointer hover:border-white/20 transition-all">
                        <option value="newest" <%= filters.sort === 'newest' ? 'selected' : '' %>>Newest First</option>
                        <option value="oldest" <%= filters.sort === 'oldest' ? 'selected' : '' %>>Oldest First</option>
                        <option value="amount_high" <%= filters.sort === 'amount_high' ? 'selected' : '' %>>Price: High to Low</option>
                        <option value="amount_low" <%= filters.sort === 'amount_low' ? 'selected' : '' %>>Price: Low to High</option>
                    </select>
                </div>
            </form>
        </section>

        <div class="space-y-6">
            <% if (orders && orders.length > 0) { %>
                <% orders.forEach(order => { %>
                    <div class="group bg-[#12141a] border border-white/10 rounded-[2rem] p-6 sm:p-8 hover:border-primary/40 transition-all duration-300 shadow-xl">
                        <div class="flex flex-col lg:flex-row lg:items-center gap-8">
                            
                            <div class="w-28 h-28 rounded-2xl bg-black border border-white/5 p-4 flex-shrink-0 flex items-center justify-center">
                                <img src="<%= order.items[0].image %>" class="w-full h-full object-contain transition-transform duration-500 group-hover:scale-110" alt="Product Image">
                            </div>

                            <div class="flex-grow space-y-4">
                                <div class="flex items-center gap-4 flex-wrap">
                                    <span class="bg-primary/20 text-primary font-bold text-xs px-3 py-1.5 rounded-lg uppercase tracking-wider">#<%= order.orderId %></span>
                                    <span class="text-gray-400 font-semibold text-xs uppercase tracking-widest">
                                        Ordered: <%= new Date(order.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %>
                                    </span>
                                </div>
                                
                                <div class="space-y-2">
                                    <div class="flex items-center gap-3 flex-wrap">
                                        <h3 class="text-white text-2xl tracking-tight"><%= order.items[0].productName %></h3>
                                        <% if (order.items.length > 1) { %>
                                            <span class="text-xs bg-white/5 text-gray-300 border border-white/10 px-3 py-1 rounded-full font-semibold uppercase">
                                                +<%= order.items.length - 1 %> More Items
                                            </span>
                                        <% } %>
                                    </div>
                                    <p class="text-xs text-gray-500 font-bold uppercase tracking-widest">
                                        Variant: <span class="text-gray-300"><%= order.items[0].color %></span> / Size: <span class="text-gray-300"><%= order.items[0].size %></span>
                                    </p>
                                </div>
                            </div>

                            <div class="flex flex-row lg:flex-col items-center lg:items-end justify-between gap-6 lg:min-w-[180px]">
                                <div class="text-left lg:text-right">
                                    <p class="text-gray-500 font-bold text-[10px] uppercase tracking-[0.2em] mb-1">Total Amount</p>
                                    <p class="text-white font-black text-2xl tracking-tighter">₹<%= order.pricing.totalAmount.toLocaleString() %></p>
                                </div>

                                <div class="flex flex-col items-end gap-4 w-full sm:w-auto">
                                    <% 
                                        let statusStyles = 'text-yellow-400 bg-yellow-400/10 border-yellow-400/20';
                                        if(order.orderStatus === 'Delivered') statusStyles = 'text-green-400 bg-green-400/10 border-green-400/20';
                                        if(order.orderStatus === 'Cancelled') statusStyles = 'text-red-400 bg-red-400/10 border-red-400/20';
                                    %>
                                    <span class="<%= statusStyles %> px-6 py-2 rounded-full text-[10px] font-extrabold uppercase tracking-widest border text-center">
                                        <%= order.orderStatus %>
                                    </span>
                                    
                                    <a href="/orders/<%= order.orderId %>" class="w-full text-center bg-white/5 hover:bg-primary text-white font-bold uppercase tracking-widest text-xs px-8 py-4 rounded-xl border border-white/10 hover:border-primary transition-all shadow-lg whitespace-nowrap">
                                        View Details
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>

                <% if (pagination.totalPages > 1) { %>
                    <nav class="flex justify-center gap-3 pt-10">
                        <% for(let i=1; i <= pagination.totalPages; i++) { %>
                            <a href="/orders?page=<%= i %>&search=<%= filters.search %>&status=<%= filters.status %>&sort=<%= filters.sort %>" 
                               class="w-12 h-12 flex items-center justify-center rounded-xl border border-white/10 <%= pagination.currentPage === i ? 'bg-primary text-white border-primary shadow-lg shadow-primary/40 scale-110' : 'text-gray-400 hover:border-white/30 hover:text-white' %> text-sm font-bold transition-all">
                                <%= i %>
                            </a>
                        <% } %>
                    </nav>
                <% } %>

            <% } else { %>
                <div class="text-center py-32 bg-[#12141a] border border-white/10 rounded-[3rem] shadow-inner">
                    <span class="material-symbols-outlined text-gray-700 text-7xl mb-6">inventory_2</span>
                    <h3 class="text-white font-bold text-xl uppercase tracking-widest mb-3">No orders found</h3>
                    <p class="text-gray-500 text-base mb-10 max-w-sm mx-auto">We couldn't find anything matching your current filters or search query.</p>
                    <a href="/orders" class="inline-flex items-center gap-3 bg-primary hover:bg-primary/80 text-white font-bold uppercase tracking-widest text-xs px-12 py-5 rounded-2xl transition-all shadow-xl shadow-primary/20">
                        Clear All Filters
                    </a>
                </div>
            <% } %>
        </div>
    </div>
</main>