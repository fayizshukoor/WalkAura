<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
    <title><%= product.name %> | WalkAura</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Outlined" rel="stylesheet"/>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: "#4F46E5",
                        "background-dark": "#0B0C10",
                        "surface-dark": "#13151A",
                        "card-dark": "#1F2937",
                        "border-dark": "#374151",
                    }
                }
            }
        };
    </script>
    <style>
        body { font-family: 'Inter', sans-serif; }
        .hide-scrollbar::-webkit-scrollbar { display: none; }
        
        /* Smooth Zoom Styles */
        .zoom-container {
            cursor: zoom-in;
            overflow: hidden;
            position: relative;
        }
        #mainImage {
            transition: transform 0.15s ease-out;
            transform-origin: center center;
            width: 75%;
            height: 100%;
            object-fit: cover; /* This makes the image fill the container */
        }
    </style>
</head>
<body class="bg-background-dark text-white antialiased">

<main class="pt-24 pb-20 px-4 max-w-[95%] xl:max-w-[75%] mx-auto">
    
    <nav class="flex mb-8 text-sm text-gray-400" aria-label="Breadcrumb">
        <ol class="inline-flex items-center space-x-1 md:space-x-3 uppercase tracking-wider text-[10px] font-bold">
            <li><a href="/" class="hover:text-white transition-colors">Home</a></li>
            <li><span class="mx-2">/</span></li>
            <li><a href="/shop" class="hover:text-white transition-colors">Shop</a></li>
            <li><span class="mx-2">/</span></li>
            <li class="text-white"><%= product.name %></li>
        </ol>
    </nav>

    <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 mb-20">
        
        <div class="lg:col-span-7 flex flex-col gap-6">
            <div class="zoom-container bg-surface-dark rounded-3xl border border-white/5 aspect-[4/3] max-h-[600px] flex items-center justify-center relative group">
                <img id="mainImage" src="<%= product.images[0]?.url %>" alt="<%= product.name %>">
            </div>
            
            <div class="grid grid-cols-4 gap-4">
                <% product.images.forEach((img, index) => { %>
                    <button onclick="updateMainImage('<%= img.url %>')" class="bg-surface-dark rounded-xl overflow-hidden aspect-square border border-white/5 hover:border-primary transition-all">
                        <img src="<%= img.url %>" class="w-full h-full object-cover">
                    </button>
                <% }) %>
            </div>
        </div>

        <div class="lg:col-span-5 flex flex-col">
            <h1 class="text-4xl font-extrabold mb-2 tracking-tight"><%= product.name %></h1>
            
            <div class="flex items-center gap-2 mb-6 text-sm">
                <div class="flex text-yellow-500">
                    <% for(let i=1; i<=5; i++) { %>
                        <span class="material-icons-outlined text-sm"><%= i <= Math.floor(averageRating) ? 'star' : 'star_outline' %></span>
                    <% } %>
                </div>
                <span class="text-gray-400">(<%= reviewCount %> reviews)</span>
            </div>

            <div class="flex items-baseline gap-4 mb-8">
                <span class="text-3xl font-bold">₹<%= finalPrice.toLocaleString() %></span>
                <% if (product.offerPercent > 0) { %>
                    <span class="text-xl text-gray-500 line-through">₹<%= product.price.toLocaleString() %></span>
                    <span class="bg-green-500/10 text-green-500 px-2 py-1 rounded text-xs font-bold"><%= product.offerPercent %>% OFF</span>
                <% } %>
            </div>

            <div class="mb-8 p-4 bg-surface-dark rounded-2xl border border-white/5">
                <p class="text-gray-400 text-sm leading-relaxed"><%= product.description %></p>
            </div>

            <div class="mb-8">
                <div class="flex justify-between items-center mb-4">
                    <span class="text-xs font-bold uppercase tracking-widest text-gray-400">Select Size</span>
                    <span class="text-xs <%= totalStock > 0 ? 'text-green-500' : 'text-red-500' %>">
                        <%= totalStock > 0 ? 'In Stock' : 'Out of Stock' %>
                    </span>
                </div>
                <div class="flex flex-wrap gap-3">
                    <% [6, 7, 8, 9, 10].forEach(size => { %>
                        <% 
                            const sizeObj = product.sizes.find(s => s.size === size);
                            const isAvailable = sizeObj && sizeObj.stock > 0;
                        %>
                        <button <%= !isAvailable ? 'disabled' : '' %> class="w-14 h-14 flex items-center justify-center rounded-xl border-2 transition-all font-bold 
                            <%= isAvailable ? 'border-border-dark hover:border-primary text-white' : 'border-transparent bg-gray-800/20 text-gray-600 cursor-not-allowed' %>">
                            <%= size %>
                        </button>
                    <% }) %>
                </div>
            </div>

            <div class="flex gap-4">
                <button <%= totalStock === 0 ? 'disabled' : '' %> class="flex-1 bg-primary hover:bg-indigo-600 disabled:bg-gray-700 disabled:cursor-not-allowed text-white font-bold py-4 rounded-2xl transition-all shadow-lg shadow-indigo-500/20 active:scale-95 flex items-center justify-center gap-2">
                    <span class="material-icons-outlined">shopping_bag</span>
                    Add to Cart
                </button>
                <button class="w-14 h-14 rounded-2xl bg-surface-dark border border-white/5 flex items-center justify-center hover:bg-white/10 transition-colors">
                    <span class="material-icons-outlined">favorite_border</span>
                </button>
            </div>
        </div>
    </div>

    <section class="mb-20">
        <h2 class="text-2xl font-bold mb-8">Customer Reviews</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <div class="bg-surface-dark p-6 rounded-3xl border border-white/5">
                <div class="flex justify-between items-start mb-4">
                    <div>
                        <h4 class="font-bold">Jane Doe</h4>
                        <p class="text-[10px] text-gray-500 uppercase tracking-widest">Verified Buyer</p>
                    </div>
                    <div class="flex text-yellow-500">
                        <span class="material-icons-outlined text-xs">star</span>
                        <span class="material-icons-outlined text-xs">star</span>
                        <span class="material-icons-outlined text-xs">star</span>
                        <span class="material-icons-outlined text-xs">star</span>
                        <span class="material-icons-outlined text-xs">star</span>
                    </div>
                </div>
                <p class="text-sm text-gray-400 italic">"Extremely comfortable for long runs. The cushioning is exactly as described."</p>
            </div>
        </div>
    </section>

    <section>
        <h2 class="text-2xl font-bold mb-8">You Might Also Like</h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            <% relatedProducts.forEach(item => { %>
                <a href="/product/<%= item.slug %>" class="group">
                    <div class="bg-surface-dark rounded-3xl p-3 border border-white/5 group-hover:border-white/20 transition-all">
                        <div class="aspect-square rounded-2xl overflow-hidden bg-[#202020] mb-4">
                            <img src="<%= item.images[0].url %>" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">
                        </div>
                        <h3 class="font-bold text-sm mb-1 truncate"><%= item.name %></h3>
                        <p class="text-gray-400 text-xs mb-2"><%= item.category.name %></p>
                        <p class="font-bold text-primary">₹<%= item.price.toLocaleString() %></p>
                    </div>
                </a>
            <% }) %>
        </div>
    </section>

</main>

<script>
    const container = document.querySelector('.zoom-container');
    const img = document.getElementById('mainImage');

    container.addEventListener('mousemove', (e) => {
        // Calculate position relative to the container
        const rect = container.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;

        img.style.transformOrigin = `${x}px ${y}px`;
        img.style.transform = "scale(2)";
    });

    container.addEventListener('mouseleave', () => {
        img.style.transformOrigin = "center center";
        img.style.transform = "scale(1)";
    });

    function updateMainImage(url) {
        img.src = url;
        img.style.transform = "scale(1)";
    }
</script>

</body>
</html>