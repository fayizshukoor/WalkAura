<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
    />
    <style>
      /* Custom Scrollbar for dark theme */
      ::-webkit-scrollbar {
        width: 8px;
      }
      ::-webkit-scrollbar-track {
        background: #000;
      }
      ::-webkit-scrollbar-thumb {
        background: #333;
        border-radius: 4px;
      }
      ::-webkit-scrollbar-thumb:hover {
        background: #444;
      }
    </style>
  </head>
  <body
    class="bg-black text-white font-sans antialiased selection:bg-blue-500 selection:text-white"
  >
   <div class="flex flex-col md:flex-row gap-6 pt-20 min-h-screen">

      
      <!-- Sidebar -->
       <%- include("../partials/sidebar") %>

      <main class="flex-1 p-6 md:p-10 lg:px-16 bg-black">
        <div class="max-w-4xl mx-auto">
          <h1 class="text-3xl font-bold mb-8 text-white">My Profile</h1>

          <div
            class="bg-[#111] border border-gray-800 rounded-3xl p-8 md:p-10 shadow-2xl relative overflow-hidden"
          >
            <div
              class="absolute top-0 right-0 w-64 h-64 bg-blue-900/10 rounded-full blur-3xl -translate-y-1/2 translate-x-1/2 pointer-events-none"
            ></div>

            <div
              class="flex flex-col md:flex-row items-start md:items-center justify-between gap-6 mb-10 border-b border-gray-800 pb-8"
            >
              <div class="flex items-center gap-6">
                
                <div class="w-24 h-24 rounded-full overflow-hidden bg-gray-800 border-2 border-gray-700 flex items-center justify-center">
                    <% if (user.profileImage?.url) { %>
                <img src="<%= user.profileImage.url %>" alt="Profile Photo" class="w-full h-full object-cover"/>
                    <% } else { %>
                <span class="material-symbols-outlined text-4xl text-gray-500">person</span>
                    <% } %>
                </div>

                <div>
                  <h2 class="text-xl font-bold text-white"><%= user.name %></h2>
                  <p class="text-gray-400 text-sm"><%= user.email %></p>
                </div>
              </div>

            <div class="flex gap-3">
              <form action="/profile/upload-photo" method="post" enctype="multipart/form-data" >
                <label class="cursor-pointer px-5 py-2.5 bg-gray-800 hover:bg-gray-700 text-xs font-medium rounded-lg transition border border-gray-700">
                   Upload New Photo
                <input type="file" name="profileImage" accept="image/*" class="hidden" onchange="this.form.submit()"/>
                </label>
              </form>

               <form action="/profile/remove-photo" method="post">
                <button type="submit" class="px-5 py-2.5 text-red-400 hover:bg-red-500/10 text-xs font-medium rounded-lg transition"> Remove </button>
              </form>

            </div>
        </div>

            <%- include('../partials/alerts') %>
            <div class="space-y-6">
              <div>
                <label
                  class="block text-xs uppercase tracking-wider text-gray-500 font-semibold mb-2"
                  >Full Name</label
                >
                <div
                  class="w-full bg-[#1a1a1a] border border-gray-700 rounded-xl px-4 py-3 text-gray-300"
                >
                  <%= user.name %>
                </div>
              </div>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label
                    class="block text-xs uppercase tracking-wider text-gray-500 font-semibold mb-2"
                    >Phone Number</label
                  >
                  <div
                    class="w-full bg-[#1a1a1a] border border-gray-700 rounded-xl px-4 py-3 text-gray-300"
                  >
                    <%= user.phone || "Not Added" %>
                  </div>
                </div>
                <div>
                  <label
                    class="block text-xs uppercase tracking-wider text-gray-500 font-semibold mb-2"
                    >Email Address</label
                  >
                  <div
                    class="w-full bg-[#1a1a1a] border border-gray-700 rounded-xl px-4 py-3 text-gray-300"
                  >
                    <%= user.email %>
                  </div>
                </div>
              </div>

             <div class="pt-6">
  <label class="block text-xs uppercase tracking-wider text-gray-500 font-semibold mb-3">
    Security
  </label>
  <div class="flex flex-wrap gap-4">
    <a
      href="/profile/change-password"
      class="px-6 py-3 bg-[#1a1a1a] hover:bg-gray-800 border border-gray-700 rounded-xl text-sm font-medium transition inline-block"
    >
      Change Password
    </a>
    <a
      href="/profile/change-email"
      class="px-6 py-3 bg-[#1a1a1a] hover:bg-gray-800 border border-gray-700 rounded-xl text-sm font-medium transition inline-block"
    >
      Change Email
    </a>
  </div>
</div>

              <div class="pt-8">
                <a
                  href="/profile/edit"
                  class="block w-full text-center bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-4 rounded-xl transition shadow-lg shadow-indigo-900/20"
                >
                  Edit Profile
                </a>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </body>
</html>


